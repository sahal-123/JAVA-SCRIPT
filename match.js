// retreaves the matches of the string against a regular expression
// match(regex)
// g - global searches(all time accessing)


let text="the rain in spain stays in the pilain"
console.log(text.match(/ain/g));
